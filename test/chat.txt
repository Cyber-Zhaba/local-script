# Вопрос 6

Найди самый часто встречающийся email в массиве wf.vars.emails

Супер. А теперь доработай код так, чтобы он возвращал не весь email, а только имя пользователя (всё, что до символа @)

# Вопрос 7

Посчитай скидку 10% от суммы wf.vars.total_price

Добавь условие: если переменная wf.vars.is_vip равна true, то скидка должна быть 20%, а не 10%.

# Вопрос 8

Посчитай сумму всех чисел в массиве wf.vars.orders

В массиве могут попадаться nil или строки. Добавь проверку типов (type(x) == 'number'), чтобы скрипт не падал с ошибкой.

# Вопрос 9

Отформатируй номер телефона из wf.vars.phone

Приведи к международному формату начиная с +7, удали все дефисы и скобки.

# Вопрос 10

Очисти значения переменных ID, ENTITY_ID, CALL из результатов RESTbody.result

# VAL 1
Сложи все суммы чеков из массива wf.vars.receipts

Данные грязные, там могут быть пустые значения nil. Игнорируй их.

Отлично, теперь примени к итоговой сумме налог 20% и верни результат.

# VAL 2

Вытащи нужные заказы из списка wf.vars.orders и верни массивом.

Нужны только те, где status == 'delivered'

# VAL 3

Преобразуй время из переменной wf.initVariables.recallTime (формат ISO) в unix-формат (timestamp в секундах).

# Вопрос 1

Увеличь каждое значение в массиве wf.vars.prices на 10%

А теперь не возвращай массив, а просто верни сумму этих увеличенных цен.

lua5.4 test_runner.lua

# Вопрос 2

Найди самую дорогую транзакцию в wf.vars.transactions

Верни не просто сумму, а строку вида: Максимальный платеж составил X руб.

lua5.4 test_runner.lua

# Вопрос 3

Выведи список доменов у почт из массива wf.vars.emails

lua5.4 test_runner.lua

Найди тот, который встричается чаще всего

lua5.4 test_runner.lua

# Вопрос 9

В датасете wf.vars.users найди самого старого и самого молодого пользователя

lua5.4 test_runner.lua

Оставь только из имена

lua5.4 test_runner.lua

# Вопрос 10

Посчитай средний возраст пользователей в wf.vars.users

lua5.4 test_runner.lua

интерактив 

lua5.4 test_runner.lua

ДЕМО 1

curl -X POST http://localhost:8000/generate -H "Content-Type: application/json" \
-d '{"prompt": "Найди максимальное число в массиве wf.vars.numbers"}'

curl -s -X POST http://localhost:8000/generate -H "Content-Type: application/json" \
-d '{"prompt": "Отфильтруй массив чисел wf.vars.numbers"}' \
| jq -r '.result' | sed 's/^lua{//; s/}lua$//' | tee script.lua && cat script.lua | lua5.4 test_runner.lua

curl -s -X POST http://localhost:8000/generate -H "Content-Type: application/json" \
-d '{"prompt": "Отфильтруй массив wf.vars.numbers: оставь только числа больше 10. Строки и nil игнорируй."}' \
| jq -r '.result' | sed 's/^lua{//; s/}lua$//' | tee script.lua && cat script.lua | lua5.4 test_runner.lua

# Демо 2

Выведи список пользователей у которых age < 18 из переменной wf.vars.users

lua5.4 test_runner.lua

Окей, теперь отфильтруй у них только поле budget. Верни массив из чисел.

lua5.4 test_runner.lua

А теперь просуммируй их бюджеты и верни итоговое число.

lua5.4 test_runner.lua

